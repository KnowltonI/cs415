#ifndef DISPENSER_H
#define DISPENSER_H

#include <string>
#include <map>

using namespace std;

class Vender {
public:
	Vender();
	~Vender();
	void checkSum(string item, int cash);
	int selectItem(String item);
private:
	map<string,int> items;	
};

#endif
